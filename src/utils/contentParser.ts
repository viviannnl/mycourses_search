export class ContentParser {
  static parseRecordingsList(html: string): Recording[] {
    // Parse the recordings list HTML
    // Return array of Recording objects
    return [];
  }

  static parseTranscript(html: string): TranscriptEntry[] {
    // Parse the transcript HTML
    // Return array of TranscriptEntry objects
    return [];
  }
} 